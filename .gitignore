# OS clutter
.DS_Store
Thumbs.db
desktop.ini

# Editor clutter
*.swp
*~
.vscode/
.idea/

# Archives / local backups — don't need to live in version control
*.zip
*.bak

# Node / build
node_modules/
dist/
*.log

# Env & local data — never commit secrets or the local DB
.env
.env.local
server/data/*.db
server/data/*.db-*
